# some notes just incase you forget git stuff\

once changes are made
git add . to add all files (or git add "path" for specific file)
git commit -m "message for commit"
git push to push
git pull to pull